build:
  version: "1.0.0"
server:
  port: 5002
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: divyanshu.dev0p@gmail.com
    password: zfkiutriyjzlbckk
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    shutdown:
      enabled: true
  info:
    env:
      enabled: true

endpoints:
  shutdown:
    enabled: true

eureka:
  instance:
    preferIpAddress: true
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      defaultZone: "http://localhost:8070/eureka/"

info:
  app:
    name: "KYC"
    description: "KYC Service"
    version: "1.0.0"
